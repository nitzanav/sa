[project]
name = "sa"
version = "0.1.0"
description = "Python project with pyenv and penv"
requires-python = ">=3.12"
readme = "AGENTS.md"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
